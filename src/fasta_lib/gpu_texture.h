#ifndef __GPU_TEXTURE_H__
#define __GPU_TEXTURE_H__

#include <vector>

#include <glad/glad.h>

namespace fst {

class GPU_Texture {
	public:
		GPU_Texture();
		~GPU_Texture();
};

} // namespace

#endif // __GPU_TEXTURE_H__